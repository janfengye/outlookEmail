## 1. 后端测试

- [x] 1.1 为 `/api/cloudflare/ai-usernames/generate` 增加测试：使用已保存且启用的 AI 配置按 `count` 返回严格等量用户名。
- [x] 1.2 为 AI 生成接口增加配置缺失、功能未启用、返回数量不足、返回数量过多、清洗后数量不匹配的失败测试，并断言不会随机补齐或截断。
- [x] 1.3 为 `/api/temp-emails/generate-batch` 增加显式 `usernames` 列表测试：数量匹配时按列表顺序创建并保存 Cloudflare 临时邮箱。
- [x] 1.4 为批量创建接口增加用户名列表数量过少、数量过多、重复、非法用户名的失败测试，并断言无效请求不会调用 Cloudflare 创建接口。
- [x] 1.5 为批量创建接口增加“用户名为空时随机生成”和“提交阶段不调用 AI 生成逻辑”的回归测试。
- [x] 1.6 为显式用户名列表的部分上游失败增加测试，断言响应包含成功列表、失败数量和带用户名的失败明细。

## 2. 后端实现

- [x] 2.1 抽出或调整 Cloudflare 用户名清洗与校验辅助逻辑，支持对显式用户名列表进行保序清洗、重复检测、格式校验和严格数量校验。
- [x] 2.2 新增 `POST /api/cloudflare/ai-usernames/generate`，只读取已保存的 Cloudflare AI 配置并返回清洗后的用户名列表。
- [x] 2.3 在 AI 生成接口中对 AI 原始结果和清洗结果执行严格数量校验，数量不等时返回错误且不做随机补齐、截断或邮箱创建。
- [x] 2.4 扩展 `/api/temp-emails/generate-batch` 支持 `usernames` 数组：空列表按数量随机生成，非空列表必须严格等于 `count`。
- [x] 2.5 移除 Cloudflare 批量创建提交阶段的隐式 AI 用户名调用，确保该接口只使用显式用户名列表或随机用户名。
- [x] 2.6 调整批量创建失败响应，让显式用户名列表模式下的失败明细包含对应用户名。

## 3. 前端实现

- [x] 3.1 将 Cloudflare 生成弹窗的用户名输入从单行 input 改为多行 textarea，并保留“一行一个用户名”的提示。
- [x] 3.2 在用户名区域右侧新增“AI生成”按钮，按钮读取当前数量并调用 `/api/cloudflare/ai-usernames/generate`。
- [x] 3.3 实现 AI 生成按钮的 loading、禁用、成功覆盖 textarea 和错误提示逻辑；成功时直接覆盖已有内容。
- [x] 3.4 调整 Cloudflare 批量创建提交逻辑：解析 textarea 非空行，非空时要求行数严格等于数量并随请求提交 `usernames`。
- [x] 3.5 确保提交创建逻辑不再因 AI 设置启用而隐式请求 AI；textarea 为空时才走随机用户名创建。
- [x] 3.6 检查生成弹窗在渠道、域名、标签、数量、用户名 textarea 和 AI 按钮组合下的布局，不影响现有设置页样式。

## 4. 验证

- [x] 4.1 更新或新增前端静态测试，覆盖 textarea、AI生成按钮、新接口调用和提交阶段不隐式 AI 的关键代码路径。
- [x] 4.2 运行 Cloudflare 临时邮箱相关后端测试，确认新增严格校验和既有渠道管理行为通过。
- [x] 4.3 运行项目现有运行时/静态检查中覆盖生成弹窗的测试，确认前端模板和脚本未破坏。
- [x] 4.4 使用本地页面手动验证 Cloudflare 生成弹窗：AI 生成覆盖、数量不匹配报错、空用户名随机生成、显式用户名创建。
